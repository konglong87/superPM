# pm-tech 输出文档模板

**用途**: 被 pm-tech SKILL.md 引用，在步骤 8 中使用此模板生成技术对接方案文档

## 文档结构

```markdown
# {产品名称} 技术对接方案

## 文档信息

- **产品名称**: {从PRD提取}
- **文档版本**: v1.0
- **创建时间**: {当前时间}
- **对接范围**: {整体方案/功能评估/接口设计}
- **生成工具**: super-pm v2.0.0

---

## 一、技术概述

### 1.1 产品背景
{从PRD提取：产品定位和目标}

### 1.2 技术目标
- 支持{核心功能}，性能要求：{具体指标}
- 扩展性：{扩展目标}，安全性：{安全要求}

---

## 二、技术栈选择

### 2.1 前端技术栈
- **框架**：{如React/Vue}，**语言**：TypeScript
- **UI组件库**：{如Ant Design}，**状态管理**：{如Redux/Pinia}
- **构建工具**：{如Vite/Webpack}

### 2.2 后端技术栈
- **语言**：{如Java/Node.js/Go}，**框架**：{如Spring Boot/Express/Gin}
- **数据库**：{如MySQL/MongoDB}，**缓存**：{如Redis}
- **消息队列**：{如RabbitMQ}

### 2.3 开发工具
版本控制 Git，CI/CD {Jenkins/GitHub Actions}，项目管理 {Jira/Notion}

---

## 三、系统架构

### 3.1 整体架构
```
客户端层 (Web/移动端/小程序/API)
    ↓
接入层 (Nginx)
    ↓
应用服务层 (用户服务/业务服务/支付服务)
    ↓
数据层 (MySQL/Redis/MongoDB/ES/MQ)
```

### 3.2 核心模块
- **用户服务模块**：注册/登录/认证/信息管理
- **业务服务模块**：{核心业务模块}
- **支付服务模块**：支付网关对接/状态管理/退款

---

## 四、功能可行性评估

### 4.1 功能列表与实现难度
| 功能 | 难度 | 风险 | 预估周期 | 备注 |
|-----|------|------|---------|------|
| 用户注册登录 | ⭐ 简单 | 低 | 3天 | 标准JWT认证 |
| 商品浏览 | ⭐ 简单 | 低 | 2天 | 标准CRUD |
| 购物车 | ⭐⭐ 中等 | 中 | 5天 | 需库存同步逻辑 |
| 订单系统 | ⭐⭐⭐ 复杂 | 高 | 10天 | 状态机复杂 |
| 支付集成 | ⭐⭐ 中等 | 中 | 5天 | 对接第三方支付 |

### 4.2 高风险功能分析
对每个高风险功能列出：风险点、缓解方案、替代方案

---

## 五、API接口设计

### 5.1 接口规范
- **基础路径**：`/api/v1`，**认证**：JWT Bearer Token
- **响应格式**：`{code, message, data}`
- **错误码**：200成功 / 400参数 / 401未授权 / 403无权限 / 404不存在 / 500服务器错误

### 5.2 核心接口列表
**用户模块**：
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/register` - 用户注册
- `GET /api/user/profile` - 获取用户信息
- `PUT /api/user/profile` - 更新用户信息

**业务模块**：
- `GET /api/products` - 获取商品列表（支持分页/筛选/排序）
- `GET /api/products/{id}` - 获取商品详情
- `POST /api/cart` - 添加到购物车
- `POST /api/orders` - 创建订单
- `GET /api/orders/{id}` - 获取订单详情

### 5.3 接口详细设计（关键接口示例）
以 `POST /api/orders` 为例展示完整请求/响应/错误码设计。

---

## 六、数据库设计

### 6.1 核心表结构
| 表名 | 核心字段 | 备注 |
|-----|---------|------|
| users | id, email, password_hash, name, phone, created_at | 用户表 |
| products | id, name, description, price, stock, category_id | 商品表 |
| orders | id, user_id, total_amount, status, created_at | 订单表 |

### 6.2 索引设计
- users: email (UNIQUE)
- products: category_id, created_at
- orders: user_id, status, created_at

---

## 七、第三方服务

| 服务类型 | 服务商 | 用途 | 月成本预估 |
|---------|--------|------|-----------|
| 支付服务 | 支付宝/微信 | 支付处理 | 交易额0.6% |
| 短信服务 | 阿里云短信 | 验证码 | 500元 |
| 对象存储 | 阿里云OSS | 文件存储 | 300元 |
| CDN加速 | 阿里云CDN | 资源加速 | 200元 |

---

## 八、非功能性需求

**性能**：页面加载<2s, API<500ms(P95), 数据库查询<100ms
**安全**：JWT认证、HTTPS、密码bcrypt加密、SQL注入/XSS/CSRF防护
**可用性**：SLA 99.9%，故障恢复<30分钟

---

## 九、部署方案

**生产环境**：用户→CDN→Nginx→应用服务器→数据库（含Redis缓存）
**CI/CD**：代码提交→自动测试→构建镜像→部署测试→人工验证→部署生产

---

## 十、项目风险与应对

**技术风险**：技术栈不熟悉、第三方服务不稳定、性能不达标、安全漏洞
**项目风险**：需求变更频繁、人员变动、周期延误

---

## 十一、开发排期建议

| 阶段 | 内容 | 时长 |
|-----|------|------|
| 基础设施 | 技术栈搭建、数据库设计、基础框架 | 1周 |
| 核心功能 | 用户模块+业务核心模块 | 3周 |
| 扩展功能 | 支付集成+第三方对接 | 2周 |
| 测试上线 | 集成测试+性能优化+上线部署 | 1周 |
| **总计** | | **7周** |

人力需求：前端1-2人，后端2-3人，测试1人

---

## 十二、下一步建议

1. **技术评审** - 与技术团队评审方案可行性
2. **/pm-feature** - 功能细节拆解，为开发做准备
3. **/pm-data** - 数据埋点方案设计
4. **/pm-risk** - 风险管控方案

---

## 附录

### A. 技术选型对比
{技术栈对比表格}

### B. 参考文档
PRD产品需求文档、原型设计方案、MVP方案

---

**文档状态**: 技术对接方案完成
**生成时间**: {时间戳}
**生成工具**: super-pm v2.0.0
```